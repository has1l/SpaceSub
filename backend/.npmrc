# Reduce memory usage during install on Railway free tier (512MB limit)
# Limit concurrent network connections to reduce peak memory
maxsockets=3
# Reduce audit overhead
audit=false
# Don't run optional dependency installs (saves memory + time)
optional=false
# Reduce fund message noise
fund=false
